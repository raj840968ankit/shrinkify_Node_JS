<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/material-design-iconic-font@2.2.0/dist/css/material-design-iconic-font.min.css"
    />
    <title>Change Password</title>
  </head>
  <body>
    <%- include('../partials/header') %>

    <section class="change-password forgot-password7">
      <div class="container7">
        <div class="change-password-content7">
          <div class="user-status">
            <% if(user) { %>
            <div class="status-badge success">
              <i class="zmdi zmdi-check-circle"></i>
              <span>You are logged in</span>
            </div>
            <% } else { %>
            <div class="status-badge warning">
              <i class="zmdi zmdi-alert-circle"></i>
              <span>You are not logged in</span>
            </div>
            <% } %>
          </div>

          <% if(formSubmitted) { %>
          <p>
            If the email that you have given us is correct, we have sent email
            to reset password to your email.
          </p>
          <% } else { %>
          <div class="change-password-form7">
            <h1 class="form-title7">Enter Your Register Email</h1>
            <form action="/forget-password" method="post">
              <div class="form-group7">
                <label for="email"></label>
                <div class="input-wrapper7">
                  <i class="zmdi zmdi-email prefix-icon"></i>
                  <input
                    type="email"
                    name="email"
                    id="email"
                    placeholder="Enter your Register Email"
                    autocomplete="false"
                  />
                </div>
              </div>

              <% if ((errors && errors.length > 0)) { %>
              <div class="error-container">
                <% errors.forEach((error) => { %>
                <p class="flash-error"><%= error %></p>
                <% }); %>
              </div>
              <% } %>

              <div class="form-group7 form-button7">
                <button type="submit" class="form-submit7">Submit</button>
              </div>
            </form>
          </div>
        </div>
        <% } %>
      </div>
    </section>

  </body>
</html>